typedef unsigned char bit8;
typedef bit8 bit64[8];
typedef bit8 bit80[10];
typedef bit8 bit128[16];
typedef bit8 bit160[20];
typedef bit8 bit240[30];
typedef bit8 bit256[32];

typedef struct {
    bit64 randNum[80][2];
    bit80 hashes[80][2];
}l64n80;

typedef struct {
    bit64 randNum[128][2];
    bit128 hashes[128][2];
}l64n128;

typedef struct {
    bit64 randNum[160][2];
    bit160 hashes[160][2];
}l64n160;

typedef struct {
    bit64 randNum[240][2];
    bit240 hashes[240][2];
}l64n240;

typedef struct {
    bit64 randNum[256][2];
    bit256 hashes[256][2];
}l64n256;

typedef struct {
    bit80 randNum[80][2];
    bit80 hashes[80][2];
}l80n80;

typedef struct {
    bit80 randNum[128][2];
    bit128 hashes[128][2];
}l80n128;

typedef struct {
    bit80 randNum[160][2];
    bit160 hashes[160][2];
}l80n160;

typedef struct {
    bit80 randNum[240][2];
    bit240 hashes[240][2];
}l80n240;

typedef struct {
    bit80 randNum[256][2];
    bit256 hashes[256][2];
}l80n256;